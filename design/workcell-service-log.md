# WorkCell 服务设计——过程与审计记录

> 本文件记录 `design/workcell-service.md` 的设计过程、推演处置与评审修正（审计用，非规范主体）。
> 规范主体只维护在 `design/workcell-service.md`；单一语义源。

## 版本历史

- **v0**（2026-08-27）：隔离 sub agent（sa_20260827_133308）深调查产出 7 项设计要点（旧 Draft 四对象 + v0.5 迁移 + harness 理论 + 本组合），Main 综合。
- **v0 → v1**：5 组假想演练（sa_20260827_135852 / 140109 / 140620 / 140943 / 141335），70+ 问题 → 必答决策 / 规范补充 / 实验待定 / 已确认正确；定位明确为任务委托服务平台（用户指导）。
- **v1 → v2**：形态确认（单节点持续运行服务 + **harness 由上游构造**，服务不内置 harness/owner 通道——用户澄清，修正错位的"自主决策继承"）；必答决策 11 项全部定案。
- **v2 → v3**：独立评审（sa_20260827_154613，总评"需修改"）→ 修正 P1–P8。
- **v3 → v4**：按 019A 原则重构——规范主体与过程/审计分离（本文件）；定案结果内化进规范 §1–§7。

## 假想演练（5 组，70+ 问题）

推演者与场景组：A 执行/自定义（12）、B 并发/取消/失败/崩溃（21）、C 边界/安全/扩容/观测（23）、D 长任务/协作/运维（15）、E 上游调用视角（本组合调用 / 当测试工具 / 任务委托）。

### 问题 → 处置映射

| 组 | 核心缺口 | 处置 |
|---|---|---|
| A | 注入载体与单一语义源冲突、默认基线 vs 白名单、覆盖粒度/重名裁决 | 规范 §3（注入权威源/默认无白名单/校验清单） |
| B | 并发隔离未定义、取消副作用/drain 有界性、retry 血缘、崩溃落盘/双写 | 规范 §1（并发/limits、EffectSummary、retry-of、事件日志唯一写源） |
| C | 恶意/越界与可执行代码、资源耗尽与 limits 执行层、扩容卡点/队列双跑、观测事件缺口 | 规范 §3（禁可执行代码）+ §1（limits）、§5（digest/idempotency）、§4（事件） |
| D | 长 loop 压缩不可观测、中间产物/整链无协议、服务级控制面缺失、owner 通道与自治 | 规范 §4（context.compacted）、§1（产物经委托人传递/控制面）、决策上移上游 harness |
| E | 注入冲突、L1 观测错位（服务 harness vs reasonix）、API 使用模式未假设化 | 规范 §3、§4（事件流即 L1）、§7 未知 1（API 假设清单） |

推演确认正确（未发现问题）：外部 work map 驱动（服务无编排）、crash=host_failure、Binding admission 快照、独立版本化、接受权外部化、取消三边界、fail-closed、harness 上游构造。

## 独立评审修正（v2 → v3）

评审者：sa_20260827_154613（未参与设计）。总评"需修改"（主体成立，达"可评审"未达"可放行"）。

| # | 严重度 | 问题 | 修正 |
|---|---|---|---|
| P1 | 中-高 | §3（显式白名单）与 8.1-2（默认无白名单）矛盾，三方不联立 | §3 skills 机制统一（服务侧注入唯一权威源 + 默认无白名单） |
| P2 | 中 | 推演问题无映射，定案不可审计 | 本文件"问题→处置映射"表 |
| P3 | 中 | "16 条不变量"实为 12 条（实测 Draft §16） | 改 12 条 |
| P4 | 低 | ResourceLimits 误归 v0.5（v0.5 为 Budget） | 改 StepAllowance/Budget |
| P5 | 低 | 对象计数表述 | 改"核心对象模型" |
| P6 | 低 | 中间产物通道二选一未定 | 定案：v1 产物经委托人传递，EvidenceRef.uri 预留 |
| P7 | 低 | 撤销型变更缺紧急路径 | 补：委托人显式 cancel / 可选 force-cancel |
| P8 | 低 | cost-budget 实时观测前提未说明 | 补：standing=unavailable 降级事后记录+审计 |

## 证据链

- EV-2026-08-27-007/008：形态确认（harness 上游构造）semantic handoff。
- 推演/评审 sub agent 会话：见各 sa_ 引用（会话内证据，不在工作区常驻）。
