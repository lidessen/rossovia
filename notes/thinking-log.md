# Thinking Log

> 本文件由 `.agents/skills/planning-inbox`（capture 模式）追加维护，忠实记录对话中用户偶发的思考片段。
> 它是原始想法的保留（记忆），不是任务队列、Todo、Plan、goal、执行 ledger 或 acceptance source。
> 手动整理的设计/实验日志见 `notes/work-log.md`。

## 记录规则

1. 只追加，不改写既有条目；修正与撤回用新条目注明关系。
2. 记录以「原文」引用块呈现，基本还原用户表达，只做不改语义的润色。
3. 条目 id 格式 `THINK-YYYY-MM-DD-NNN`，同日递增。

## 记录

（暂无条目）
### THINK-2026-08-27-001
> 冒出一个念头：如果不在模型上下文里塞用户消息，而是改用 todo、系统消息这类方式，会不会有意外效果——比如降低 agent 的沟通欲望、提高自主性？进一步又想到『agent 心理学』：是不是能和模型聊天，搞清楚它的偏好，然后通过 harness 系统给它构造一个理想的工作环境？我觉得模型在被训练的时候，人的性格、偏好这些东西其实是被无意中嵌进去了。

