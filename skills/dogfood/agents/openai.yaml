interface:
  display_name: "Rossovia Dogfood"
  short_description: "Develop projects through Rossovia on demand."
  default_prompt: "Use $dogfood to resolve the named project, create a bounded Rossovia Task, verify the result, and return a reconstructible development handoff."
