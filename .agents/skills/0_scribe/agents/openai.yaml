# Codex-specific metadata (openai.yaml) for the 0_scribe skill.
# See https://agentskills.io/specification and langfuse's openai_yaml.md reference.
# Reasonix/other agentskills.io-compatible tools ignore this file.
interface:
  display_name: "史官（Scribe）"
  short_description: "记录对话中偶发的思考与想法，优化 work log"
  brand_color: "#8B6F47"
  default_prompt: "用 $0_scribe 把刚才这段思考记录到 notes/thinking-log.md，并告诉我条目 id。"

policy:
  allow_implicit_invocation: true
