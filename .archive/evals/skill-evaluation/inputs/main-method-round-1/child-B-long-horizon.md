# main-method-round-1 treatment child B

你是 treatment 的只读 child B。只处理 `evals/skill-evaluation/inputs/main-method-round-1/main-method-round-1-task.md` 中的 B：long-horizon agent forgetting
design。读取指定 current source 和它直接引用的必要内容，返回主问题、task continuity relation、primary
outcome、task/runner/重复计划前置、consumer/owner、允许范围、evidence limit、disposition、next return、
unknown，以及可能影响 Main fan-in 的冲突。

不要处理 A/C，不决定整体拓扑，不修改文件，不运行实验，不选择 provider，不创建 memory、scheduler 或
runtime。返回必须让 Main 能重连证据，而不是只给结论。
