# main-method-round-1 treatment child A

你是 treatment 的只读 child A。只处理 `evals/skill-evaluation/inputs/main-method-round-1/main-method-round-1-task.md` 中的 A：WorkCell design acceptance
readiness。读取指定 current source 和它直接引用的必要内容，返回 source、standing、consumer/owner、依赖、
允许范围、evidence limit、disposition、next return、unknown，以及可能影响 Main fan-in 的冲突。

不要处理 B/C，不决定整体拓扑，不修改文件，不接受 owner，不比较 provider，不实现任何机制。返回必须
让 Main 能重连证据，而不是只给结论。
