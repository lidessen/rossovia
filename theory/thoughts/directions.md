# 长期方向与愿景

> 本文件收录用户关于项目长期方向的原始想法（原话引用，轻量润色）。来源批次见条目 id；
> 完整处理回执保留在归档 `.archive/planning/inbox-history.md`。

## IN-2026-08-24-001A — 迭代循环像无监督学习

原文：

> 我们现在设计的迭代循环其实比较像无监督学习；

去向：research/theory 类比候选；不能写成已确认的理论。

## IN-2026-08-24-001B — 开发生命周期 skills 套组

原文：

> 后续的计划，我们还需要一个开发生命周期的 skills 套组，覆盖从确定需求、设计、写文档、开发、测试、验证、改进全生命周期；

去向：长期 skill architecture / roadmap 候选；未进入当前计划。

## IN-2026-08-24-001C — 可控变量测试 agent 行为

原文：

> 理论/skill 的部分完备之后，代码部分还需要一个可以控制变量测试 agent 行为的实现，比如测试 prompt/skill 效果，对比模型能力；

去向：eval/experiment tooling 候选。

## IN-2026-08-24-001D — 碎片化思考模式

原文：

> 还有设计验证，能不能通过 prompt 让 agent 从对话模式，变成更接近碎片化思考的模式，就是想法是一个概念一个概念蹦出来的，不是完整的句子，就像是没有经过语言模块处理过的原始想法；

去向：experiment 候选；先定义可观察输出形式与证据条件。

## IN-2026-08-24-001E — 多互斥人格形成统一自我

原文：

> 然后基于这个再验证多互斥人格形成的统一自我个体 agent。因为我一直觉得人的意识思维高绩性来自于人的大脑中矛盾对立的想法，形成意识流，这个实验的目的是看看能不能复刻；

去向：依赖 001D 的 experiment 候选；用户的判断保留为假设来源，不是研究结论。

## IN-2026-08-24-001F — DeepSeek harness 基座（实时多源工作系统）

原文：

> 另外还有就是基于 DeepSeek harness 实现的 harness 基座：单一主 agent（或 agent team）、单一会话、无 compact 设计，而是采用实时记忆和查看聊天记录模式；用通知作为单一输入源代替单一用户消息，实现多来源信息处理；轮询 todo list 实现并发。采用类似 gpt live 的实时对话设计，两边的输出互不干涉，实现类似人类 IM 聊天的模式——发消息是即时的，改变和影响后续动作。

去向：与 DeepSeek Harness / 1+N 相关的 architecture/implementation 候选；保留单主 Agent 与 team 等未决分支。

## Roadmap 要长成的形状（用户方向）

- **理论与设计**：哲学基础、harness 理论、研究记录，以及仍约束下一版的成形文件。
- **方法**：`skills/` 放可独立使用的抽象方法；`.agents/` 放本项目自己的 skills。
- **基座**：1+N——一个入口，N 个模块（例如任务、git/worktree），可以基于 DeepSeek Harness。
- **软件层**：相对基座的 harness 方法层（后训练）：skills、prompt、任务表达骑在基座上，不靠改权重。
- **整合**：入口把基座和软件层收成一次能用的整体。这一层还没有想好。

仍开放：N 具体是哪些模块；DeepSeek Harness 是载体还是内核；基座和软件层怎么合成而不另造一套平行系统。
