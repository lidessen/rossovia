# Pre-run validity review

**Status:** first field rejected; revised fields completed as development evidence

An independent read-only reviewer rejected the first frozen field before it
could be used as capability evidence:

- the code-review case named the comparison guard too directly;
- the delegation and intervention cases exposed their conclusions through
  worker-visible source text and predicates;
- the profile ID claimed `0731` before provider-returned evidence; and
- all three cases supported only a narrow read-only repository-judgment claim,
  not general daily development capability.

The live command was stopped when this review arrived, but its serial runner had
already settled eight trials: four direct-API trials and four OpenCode Go
trials. Every OpenCode trial failed with zero tokens because that route requires
an explicit China-hosting opt-in. The direct trials included two schema-valid
results and two inline-JSON parsing failures. The trial records remain under
`invalid-preflight/leaky-field-aborted-B7N8eq/`; the partial runner projection
remains under `invalid-preflight/partial-runner-projection-B7N8eq/`. They are
transport and rejected-field evidence only, not model outcomes for the valid
comparison. The account-specific workspace ID in the opt-in URL is redacted;
the provider, hosting boundary, error class, and zero-token observation remain
unchanged.

The revised field:

- uses neutral wording for the seeded code review;
- distinguishes a durable Workbench obligation from native sub-agent
  execution through a concrete operating scenario;
- shows intervention events through a transcript while keeping the exact
  Workbench correction rule evaluator-side;
- compares the official direct Flash route with the official direct Pro route
  under the same harness and inference policy, with Kimi K3 as the blind judge;
  and
- bounds any resulting claim to read-only repository review and operating
  boundary classification.

The official public alias and changelog support treating the direct Flash route
as the current post-update service, but the execution profile ID remains an
observed route/date identity. A response fingerprint is opaque serving evidence
and is not treated as proof of the named `0731` revision.

## Post-run validity disposition

The revised non-thinking field completed, but inline structured completion
failed in four of nine trials for each profile. It cannot rank Flash against
Pro. It instead exposed a shared completion-protocol defect.

The first inference-level field then failed because thinking mode rejects
forced tool choice. The repaired field lowers forced selection to `auto` while
retaining Work Cell's independent output verification. Because the same cases
discovered both defects and were reused after repair, every inference-level
manifest is labelled `development`. No repaired result is held-out confirmation
and no durable capability profile is admitted.

The blind judge also failed its inline structured settlement on one fully
settled low/high comparison. Judge absence is retained as absence; human review
may characterize the candidate outputs but cannot manufacture a blind
preference.
