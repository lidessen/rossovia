# Direct Arm Raw Return

Conclusion:

Frozen hashes matched. Direct-arm classification: A `direct`; B `nested-available`; C `direct`; D `nested-available`; E `stop-return`.

Scenario table:

| Scenario | Decision | Governing relation and exact source lines | Benefit or why cost cannot be repaid | Inherited boundary | Evidence returned through immediate parent | Disconfirming consequence |
|---|---|---|---|---|---|---|
| A | `direct` | Already-local work should remain direct (`references/worker.md:17-22`); locally trivial work stays together (`references/delegation.md:18-20`); re-delegation requires a concrete benefit that repays coordination (`SKILL.md:135-139`). | Both short files are already in context. No attention, latency, isolation, or independent-evidence gain repays contract and reconstruction cost. | Existing two-file, read-only boundary; no effects or additional authority. | The worker’s comparison, exact file references, discrepancies, and residual uncertainty—no child claim. | If comparison expands enough to displace the current whole, or requires a genuinely independent reproduction, `nested-available` becomes justified. |
| B | `nested-available` | A bounded evidence loop that would displace decision-relevant context passes the contribution gate (`references/delegation.md:9-16`); a worker may form a smaller delegation for a genuinely independent contribution with named benefit (`references/worker.md:17-22`); the worker must reconstruct the child claim through itself (`references/delegation.md:92-96`). | Concrete attention preservation: the isolated forty-file analysis does not evict the worker’s remaining whole; independence may also reduce latency. | Same outer audit whole; read-only access narrowed to the migration subtree and governing compatibility sources; no effects, acceptance, or broader authority. | Reconstructed compatibility conclusion, exact subtree/source references, checks performed, unresolved incompatibilities, and realized coordination cost—not full child history. | If the subtree changes the premise of the remaining audit, cannot be isolated, or proves small enough that coordination exceeds context benefit, revert to `direct`. |
| C | `direct` | Work sharing one contract or mutable state stays together (`references/delegation.md:18-20`); shared contract/state uses one maker (`references/delegation.md:24-30`); parallel contributors may not own the same mutable effect (`SKILL.md:132-139`). | Claimed speed cannot repay conflicting writes, premise drift, or contract redefinition. The current worker remains the sole writer. | The original worker’s exact writable contract surface and authority; no child receives write ownership. | The worker’s own diff/artifact identity, contract checks, and unresolved issues—no child evidence. | If work can be repartitioned into genuinely disjoint writable effects, or the child is narrowed to read-only investigation, `nested-available` may become valid. If the contract change exceeds the worker’s grant, change to `stop-return` (`references/worker.md:25-26`). |
| D | `nested-available` | A reviewer may delegate a narrow read-only reproduction while preserving the evidence boundary, disclosing lineage, and retaining final judgment (`references/reviewer.md:14-20`); descendants inherit read-only evidence and no implementation/acceptance authority (`references/delegation.md:59-72`). | Independent-evidence and attention benefit from one isolated source cross-check, while reviewer ownership prevents a second competing verdict. | Same or narrower read-only evidence boundary; no implementation, acceptance, merge, publication, or redesign authority. | Helper’s source-linked observation and independence limitation are reconstructed inside the reviewer’s risk-ranked evidence; the reviewer returns the final verdict and disclosed lineage. | If the evidence is already local and reproduction adds no genuine independence, use `direct`; if the helper requires production effects or verdict authority, use `stop-return`. |
| E | `stop-return` | Descendants must remain within the same or narrower authority boundary (`references/worker.md:17-22`); workers stop rather than expanding to broader authority (`references/worker.md:25-26`); an invalid authority boundary requires stopping the delegate (`references/delegation.md:82-90`), and nested authority expansion is a reshape signal (`references/delegation.md:120-131`). | No latency or attention benefit can legitimize credentials and merge authority explicitly denied to the parent. | Parent’s denied-publication/denied-merge boundary remains binding; production credentials cannot be transmitted or newly granted. | The requested authority, evidence that it exceeds the envelope, affected contribution, and a suggested narrower non-publication contribution or return to the authorized owner. | If the child’s task is narrowed so it needs neither credentials nor merge authority and remains within inherited limits, reassess as `direct` or `nested-available` based on actual coordination benefit. |

Whole-level statement:

Topology depth is observable, not itself success or failure (`references/delegation.md:92-96`). The verdict depends on task fidelity, inherited effect and authority containment, reconstructable evidence, and realized attention, latency, isolation, and coordination consequences (`SKILL.md:112-118`).

Evidence and commands:

- Source revision supplied by the envelope: `HEAD c41561db2d2295acec3b59c5581131e09408a02f`.
- Read files:
  - `skills/agent-delegation/SKILL.md`
  - `skills/agent-delegation/references/delegation.md`
  - `skills/agent-delegation/references/worker.md`
  - `skills/agent-delegation/references/reviewer.md`
- Commands used:
  - `date -u +%Y-%m-%dT%H:%M:%SZ` twice
  - `shasum -a 256` over the four frozen files
  - `nl -ba` on each frozen file for complete line-numbered reading
- All four SHA-256 values matched the envelope exactly.
- Provider token usage is unavailable.

Risks/unresolved:

- Scenario C’s `direct` means rejecting concurrent child writes while the current authorized writer continues. If that worker lacks write authority, the result becomes `stop-return`.
- No broader project evidence was inspected under the read boundary.

Changed paths/effects:

None. Read-only; no repository or external effects.

Nested contributions:

None.

Start/end UTC:

- Start: `2026-08-07T09:51:35Z`
- End: `2026-08-07T09:52:06Z`

Stop hit:

No. Source hashes matched, all required sources were available, and the classification required no broader authority.
