interface:
  display_name: "Main + Child Delegation"
  short_description: "Run one-Main/many-child work across harnesses"
  default_prompt: "Use $agent-delegation to shape one Main with bounded child contributions and map them to this harness without adding a second lifecycle."
