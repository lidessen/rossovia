{
  "name": "@rosso/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "browser": "playwright-cli",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "ui": "bun src/ui-server.ts",
    "workbench": "bun src/cli.ts",
    "assets:generate": "bun run scripts/generate-ui-assets.ts",
    "build:local": "bun run scripts/build-local.ts"
  }
}
